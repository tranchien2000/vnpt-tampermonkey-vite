# Pi Session Sync Log
*Cập nhật cuối cùng: 2026-04-17 16:30 (Máy 1)*

## 1. Trạng thái hiện tại (Current Context)
- **Đang làm dở**: Khôi phục 2 tính năng quan trọng sau sự cố đơ UI:
    1. **Zoom Alt + Số** (85%, 100%, 115%).
    2. **Layout Thẳng hàng & Chọn cả dòng** trong bảng Fields.
- **Vấn đề đã xử lý**: 
    - Nâng cấp `mstService.js` với API dự phòng Xinvoice và cơ chế timeout 5s.
    - Sửa lỗi nút tra cứu MST (🔍) bị "liệt" sau khi reload trang.
    - Dọn dẹp dự án (xóa file rác, nén `PROJECT_MEMORY.md`).
    - Thiết lập Knowledge Graph nội bộ (`scripts/generate_graph.cjs`).
- **Lỗi đang gặp**: Đã khôi phục UI về bản v1.6.27 nhưng cần viết lại tính năng Zoom và Layout một cách sạch hơn để tránh làm đơ script.

## 2. Các thay đổi chưa Commit/Release
- Nâng cấp Field Linker (Smart Selector + Tooltip gợi ý).
- Nâng cấp MST Service (Fallback & Retry).

## 3. Bước tiếp theo cần thực hiện (Next Steps)
1. Viết lại CSS thẳng hàng cho `fields.js` (Sử dụng Strict Flexbox).
2. Viết lại logic Zoom bằng phím `Alt + 1/2/3` trong `widget.js`.
3. Viết lại logic click chọn cả dòng trong `row.js`.
4. Chạy `npm run release` để chốt bản v1.6.29.

---
*Ghi chú: Khi sang máy 2, Pi hãy đọc file này để tiếp tục công việc tại Bước 3.*
